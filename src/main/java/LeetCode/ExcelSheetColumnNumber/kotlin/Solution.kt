package LeetCode.ExcelSheetColumnNumber.kotlin


fun main() {
  /*  val solution = Solution()
    val columnTitle = "CA"
    val columnNumber = solution.titleToNumber(columnTitle)
  //  println("Column title for $columnTitle is $columnNumber")
}

class Solution {
    fun titleToNumber(columnTitle: String): Int {
        var result = 0
        for (i in 0 until columnTitle.length) {
            val charValue = columnTitle[i] - 'A' + 1
            result = result * 26 + charValue
        }
        return result
    }

   */
}